# Phase 3 stub deps
fastapi
uvicorn[standard]
pydantic
python-multipart

# Phase 9 - real model serving (CLAUDE.md section 2)
torch --index-url https://download.pytorch.org/whl/cpu   # CPU-only wheel
transformers        # QA's fine-tuned InLegalBERT span head
scikit-learn        # bail (XGBoost+TF-IDF), summarization (sentence classifier)
xgboost              # bail final model
spacy                # NER
sentence-transformers  # retrieval query embedding (MiniLM)
faiss-cpu            # retrieval index
scipy                # sparse feature hstack (bail)
joblib               # loading trained sklearn/xgboost artifacts
pytesseract          # OCR wrapper around the Tesseract binary
Pillow               # image decoding for OCR
shap                 # SHAP factor attribution for bail predictions
